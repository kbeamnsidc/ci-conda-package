classes:
  - nsidc_miniconda

nsidc_miniconda::install:
  /opt/miniconda:
    python_version: "2"
    build: true
    config:
      channels:
        anaconda_nsidc_dev: true
        extra: []
    packages:
      - invoke
